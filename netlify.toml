[build]
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.17.0"

[functions]
  external_node_modules = ["jsdom", "canvas", "@mozilla/readability"]
  node_bundler = "esbuild"
